[package]
name = "parser"
version = "0.1.0"
edition = "2021"

[dependencies]
lexer = { path = "../lexer" }
syntax-tree = { path = "../syntax-tree" }

[build-dependencies]
chrono = { workspace = true }
